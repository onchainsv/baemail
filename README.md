# Baemail

```
npm install --save baemail
```

```
import { createMessage } from 'baemail'

const message = await createMessage({
  amount,
  html,
  to,
  subject,
  fromName,
  fromPaymail,
  fromPki,
  imb
})

```
